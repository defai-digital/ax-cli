# pnpm-specific configuration
# Note: npm will warn about these settings but they are intentional for pnpm
# Development uses pnpm; users install via: npm install -g @defai.digital/ax-cli

strict-peer-dependencies=true
auto-install-peers=true
prefer-frozen-lockfile=true
