name: Publish to NPM

on:
  push:
    tags:
      - 'v*.*.*'  # Triggers on version tags like v1.0.0, v0.0.35, etc.
  workflow_dispatch:  # Allows manual triggering from GitHub UI
    inputs:
      version:
        description: 'Version to publish (leave empty to use package.json version)'
        required: false
        type: string

jobs:
  test:
    name: Test Before Publish
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build schemas package
        run: pnpm run build:schemas

      - name: Run linter
        run: pnpm run lint
        continue-on-error: true  # Don't fail on lint warnings

      - name: Run type check
        run: pnpm run typecheck

      - name: Run tests
        run: pnpm test

      - name: Build project
        run: pnpm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: |
            dist/
            packages/schemas/dist/
          retention-days: 1

  publish:
    name: Publish to NPM
    needs: test
    runs-on: ubuntu-latest

    permissions:
      contents: write
      id-token: write  # Required for npm provenance

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Fetch all history for changelog generation

      # Use npm (not pnpm) for publish to avoid hard link issues
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies with npm
        run: npm install

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: .

      - name: Configure Git
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

      - name: Bump version (if manual trigger)
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.version != ''
        run: |
          npm version ${{ github.event.inputs.version }} --no-git-tag-version
          git add package.json
          git commit -m "chore: bump version to ${{ github.event.inputs.version }}"
          git tag "v${{ github.event.inputs.version }}"

      - name: Get package version
        id: package-version
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Publishing version: $VERSION"

      - name: Check if version exists on NPM
        id: check-version
        run: |
          PACKAGE_NAME=$(node -p "require('./package.json').name")
          if npm view $PACKAGE_NAME@${{ steps.package-version.outputs.version }} version 2>/dev/null; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "⚠️ Version ${{ steps.package-version.outputs.version }} already exists on NPM"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "✅ Version ${{ steps.package-version.outputs.version }} is new"
          fi

      - name: Publish to NPM (with provenance)
        if: steps.check-version.outputs.exists == 'false'
        run: npm publish --provenance --access public --tag latest
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Check if GitHub Release exists
        if: steps.check-version.outputs.exists == 'false' && github.event_name == 'push'
        id: check-release
        run: |
          if gh release view ${{ github.ref_name }} >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "⚠️ GitHub Release ${{ github.ref_name }} already exists"
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "✅ GitHub Release ${{ github.ref_name }} does not exist yet"
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub Release
        if: steps.check-version.outputs.exists == 'false' && github.event_name == 'push' && steps.check-release.outputs.exists == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref }}
          name: Release ${{ steps.package-version.outputs.version }}
          body: |
            ## AX CLI v${{ steps.package-version.outputs.version }}

            ### Installation
            ```bash
            npm install -g @defai.digital/ax-cli@${{ steps.package-version.outputs.version }}
            ```

            ### Changes
            See [GitHub Releases](https://github.com/defai-digital/ax-cli/releases) for details.

            ### NPM Package
            https://www.npmjs.com/package/@defai.digital/ax-cli/v/${{ steps.package-version.outputs.version }}
          draft: false
          prerelease: false
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Skip GitHub Release (already exists)
        if: steps.check-version.outputs.exists == 'false' && github.event_name == 'push' && steps.check-release.outputs.exists == 'true'
        run: |
          echo "⏭️ Skipping GitHub Release creation - release ${{ github.ref_name }} already exists"
          exit 0

      - name: Notify success
        if: steps.check-version.outputs.exists == 'false'
        run: |
          echo "🎉 Successfully published @defai.digital/ax-cli@${{ steps.package-version.outputs.version }} to NPM!"
          echo "📦 NPM Package: https://www.npmjs.com/package/@defai.digital/ax-cli"
          echo "🏷️ Version: ${{ steps.package-version.outputs.version }}"

      - name: Skip publishing (version exists)
        if: steps.check-version.outputs.exists == 'true'
        run: |
          echo "⏭️ Skipping publish - version ${{ steps.package-version.outputs.version }} already exists on NPM"
          exit 0

  notify-failure:
    name: Notify on Failure
    needs: [test, publish]
    if: failure()
    runs-on: ubuntu-latest

    steps:
      - name: Notify failure
        run: |
          echo "❌ Publish workflow failed!"
          echo "Please check the logs and fix any issues before retrying."
